name: unisync_cli
description: UniSync command-line interface for headless/server file synchronization.
version: 0.1.0

environment:
  sdk: ^3.11.4

dependencies:
  args: ^2.6.0
  unison_core:
    path: ../packages/unison_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
